services:
  chromadb:
    image: "chromadb/chroma"
    ports:
      - "8000:8000"
  # backend:
  #   build: 
  #     context: .
  #     dockerfile: Dockerfile.backend
  # frontend:
  #   build: 
  #     context: .
  #     dockerfile: Dockerfile.frontend